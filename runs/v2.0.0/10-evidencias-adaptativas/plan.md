# Plan F05

Centralizar la política en `feature-contract.ps1`, con fallback legacy; validar SUMMARY y JSON máquina; hacer que `ready-for-pr.ps1` consuma la política y enumere sólo archivos presentes; mantener `start-work-unit.ps1` mínimo; cubrir escenarios con pytest. No se altera ASSESS, SDD, CONVERGENCE ni historia.
