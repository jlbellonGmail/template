# Plan

Extender los scripts existentes con un snapshot común derivado en memoria. Mantener el formato legible y campos históricos para compatibilidad. El JSON es opt-in (`-Json`/`-MachinePath`) y no se crea un servicio ni un archivo runtime implícito. Añadir documentación mínima y probar escenarios históricos mediante pytest y comprobaciones directas.
