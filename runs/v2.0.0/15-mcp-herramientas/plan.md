# Plan F10

Validar `.agentic/mcp.json` con JSON Schema; implementar un evaluador local que
consuma `scripts/security-policy.ps1`; devolver `ALLOW`, `DENY`, `FALLBACK` o
`BLOCKED` sin invocar red; cubrir escenarios positivos y negativos con mocks;
documentar la decisión de mantener cero servidores y regenerar adaptadores.
