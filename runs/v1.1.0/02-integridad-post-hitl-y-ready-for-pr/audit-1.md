```yaml
status: approved
attempt: 1
feedback:
  - "Observación no bloqueante (GAP A / plan.md 2.1, spec.md caso borde 'Paginación de reviews'): confirmar en la implementación real el formato exacto de salida de `gh api ... --paginate` para un endpoint que devuelve un array (la duda es si concatena en un único array JSON válido o imprime páginas concatenadas que requerirían `--slurp` para unificarse). El plan no lo aclara explícitamente y no hay ninguna tarea/test que ejercite el caso multi-página real (T-06 cubre 'múltiples reviews' pero dentro de una sola página simulada). No es motivo de rechazo porque el caso de una sola página (la inmensa mayoría de PRs reales) queda bien cubierto por AC-8/T-05/T-06, pero builder-agent debería verificar el comportamiento real de `gh api --paginate` (o usar `--slurp` si hace falta) antes de dar por cerrado el caso borde de paginación de spec.md."
  - "Observación menor (plan.md 2.2, último bullet vs T-08/T-09): el plan solo justifica que la rama Milestone 'todos ya Ready' debe ejecutar la nueva validación temprana antes de continuar a push/PR, pero no dice lo mismo de la rama análoga en Feature (`ready-for-pr.ps1` líneas 162-164, cuando el slug ya está en `[-]`). No es un bug -- la llamada final ya existente con `-RequireReadyRoadmap` (línea 179) ya cubre ese caso en ambas ramas porque corre fuera del if/else y antes del push -- pero conviene que builder-agent aplique el mismo criterio de forma simétrica en ambos modos para que el diseño quede consistente y no dependa implícitamente de que 'ya está cubierto por la llamada final'."
```

## Verificación de coherencia y trazabilidad

- Cada `AC-N` (1 a 15) de `spec.md` tiene cobertura explícita en `plan.md` (secciones 2.1/2.2/2.3, 6) y en `tasks.md` (T-01 a T-23), y cada tarea de `tasks.md` traza a un `AC-N` real existente en `spec.md`. No se encontró alcance inventado en `plan.md`/`tasks.md` que no esté respaldado por un `AC-N`.
- El "Explícitamente NO incluye" de `spec.md` (branch protection real, tamaño de MILESTONE, workflows de Actions, router de modelos, reescritura retroactiva de PRs, reintentos automáticos de aprobación) es respetado punto por punto en la sección 1 de `plan.md` ("No se toca ... ningún AC de esta spec lo requiere"). No hay scope creep.

## Verificación del diagnóstico técnico contra el código real

- **GAP A**: confirmado que `scripts/complete-approved-pr.ps1` pide `--json number,state,baseRefName,headRefName,url,reviewDecision` (sin `headRefOid`, línea 277) y que el chequeo `$pr.reviewDecision -ne "APPROVED"` (líneas 293-295) antecede a `Wait-PrChecks` (línea 298) — coincide exactamente con las líneas que cita `plan.md` ("línea ~293-295"). El diagnóstico y el punto de inserción propuesto son correctos.
- **GAP B**: confirmado que tanto la rama Feature (líneas 155-180) como la Milestone (líneas 128-153) de `scripts/ready-for-pr.ps1` mutan y commitean `ROADMAP.md` **antes** de la única llamada existente a `Assert-FeatureContract`/`Assert-WorkUnitContract -RequireReadyRoadmap` (líneas 179 y 153 respectivamente). El diagnóstico del orden no transaccional es correcto.
- **GAP C**: confirmados los literales `$($info.RunDir)/audit-N.md`, `.../test-report-N.md`, `.../code-review-N.md` en ambas ramas del bloque `$evidenceSection` (líneas 216-218 y 233-235). Correcto.
- **Afirmación clave sobre `-RequireReadyRoadmap`**: se leyó `scripts/feature-contract.ps1` (`Assert-WorkUnitContract`, líneas 407-477) y se confirmó que el switch es puramente aditivo: sin él, ya valida `decision.md`, docs técnica/usuario, último veredicto aprobado (`Assert-LatestVerdictApproved`) e índices (`Assert-IndexLink`) para Feature y Milestone; con él, agrega exclusivamente el chequeo de estado de `ROADMAP.md`/`Assert-RoadmapItemsTransition`. La decisión de diseño de no tocar `feature-contract.ps1` y reordenar solo `ready-for-pr.ps1` es válida y verificable.
- Confirmado que `Assert-RoadmapItemsTransition` (`scripts/workunit-lib.ps1` líneas 99-104) efectivamente "no muta, solo valida", como afirma `plan.md`.
- Confirmado que los nombres de funciones/propiedades citados por `plan.md`/`tasks.md` (`Get-LatestVerdictArtifact`, `Get-WorkUnitInfo`, `$info.RunDir`, `$info.Items`, `item.TechnicalDoc`/`UserDoc`, `$info.Decision`) existen tal cual en `workunit-lib.ps1`/`feature-contract.ps1`.
- Confirmado que todos los tests preexistentes citados por nombre en `plan.md`/`tasks.md` (`test_complete_approved_pr_merges_after_green_checks`, `test_complete_approved_pr_returns_builder_feedback_when_checks_fail`, `test_complete_approved_pr_requires_hitl_approval_before_checks`, `test_complete_approved_pr_blocks_wrong_base_branch`, `test_complete_approved_pr_is_rerunnable_after_checks_turn_green`, `test_ready_for_pr_creates_pr_after_expected_missing_pr`, `test_ready_for_pr_reuses_existing_pr_without_duplicate`, `test_ready_for_pr_blocks_real_gh_error`) existen realmente en los archivos de test, igual que `write_fake_gh`/`FAKE_GH_MODE`/`FAKE_GH_LOG`, `make_contract_repo` y `prepare_ready_repo`. La estrategia de tests de `plan.md`/`tasks.md` sigue esos patrones existentes sin proponer debilitar ninguna aserción.
- Verificada la razonabilidad del análisis sobre por qué las modificaciones al fake `gh` (T-04) no rompen `checks_fail`/`review_required`/`wrong_base`: `review_required` y `wrong_base` lanzan excepción antes de llegar al nuevo chequeo de stale (porque ese chequeo se inserta después de la validación de `reviewDecision`), y `checks_fail` necesita que el fake responda con `headRefOid` coincidente para llegar a `Wait-PrChecks` — el plan lo contempla explícitamente.

## Documentación y trazabilidad obligatoria

- AC-12, AC-13, AC-14, AC-15 exigen explícitamente `docs/tecnica/integridad-post-hitl-y-ready-for-pr.md`, `docs/usuario/integridad-post-hitl-y-ready-for-pr.md`, `runs/v1.1.0/02-integridad-post-hitl-y-ready-for-pr/decision.md` y enlaces exactos en ambos índices — todo presente, sin faltantes que ameriten rechazo automático.
- No hay stack/backend/dependencia nueva no declarada (spec y plan lo confirman explícitamente y es consistente con el código real: solo se toca el motor del circuito, no arquitectura de producto).
- No hay contenido de negocio inventado (spec es puramente técnica sobre el propio circuito agéntico).

## Veredicto

`approved`. El diagnóstico técnico de los tres gaps es correcto y verificable línea por línea contra el código real, la afirmación sobre `-RequireReadyRoadmap` siendo aditivo es exacta, la trazabilidad `AC-N` ↔ `plan.md` ↔ `tasks.md` es completa en ambas direcciones, el alcance respeta los límites declarados sin scope creep, la estrategia de tests sigue los patrones existentes sin debilitar ningún test (AC-11/T-07/T-13), y los cuatro artefactos de documentación/trazabilidad obligatorios están exigidos como criterios de aceptación. Las dos observaciones del feedback son notas para que `builder-agent` las tenga presentes durante la implementación, no bloquean el paso a `builder-agent`.
