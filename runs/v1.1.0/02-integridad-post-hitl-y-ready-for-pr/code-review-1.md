```yaml
status: rejected
attempt: 1
feedback:
  - "Bug real en GAP C (AC-6/AC-7 y el caso borde explícito de spec.md 'Encoding / caracteres especiales en el path del artefacto': 'el nombre de archivo de evidencia insertado en el body de la PR debe seguir el mismo formato de ruta relativa (runs/<slug>/audit-N.md) ... sin rutas absolutas del entorno del agente'). `Get-LatestVerdictArtifact` (scripts/feature-contract.ps1:105) construye `$path = $latest.File.FullName`, y `System.IO.FileInfo.FullName` SIEMPRE devuelve una ruta absoluta resuelta contra el directorio actual del proceso (p. ej. `D:\proyectos\worktrees\02-integridad-post-hitl-y-ready-for-pr\runs\02-integridad-post-hitl-y-ready-for-pr\audit-2.md`, o el path absoluto de un runner de CI), nunca `runs/<slug>/audit-2.md`. `scripts/ready-for-pr.ps1:218-220` usa ese `.Path` directamente en `$evidenceSection` (líneas 241-243, 258-260) y en el checklist (línea 277), quedando inconsistente con el resto de la sección (que sí usa `$info.RunDir` relativo para Spec/Plan/Tasks/Decision) y filtrando la ruta absoluta del filesystem de quien corrió el script en el cuerpo público de la PR en GitHub."
  - "docs/tecnica/integridad-post-hitl-y-ready-for-pr.md:165-167 afirma explícitamente lo contrario de lo que hace el código: 'Get-LatestVerdictArtifact ya devuelve una ruta relativa consistente con el resto de la sección de evidencias (runs/<slug>/...), sin rutas absolutas del entorno del agente que la generó.' Esa afirmación es falsa contra la implementación real (ver punto anterior) — la documentación técnica no describe el diff final, describe una versión aspiracional incorrecta. plan.md 2.3 tiene la misma afirmación errónea, que parece haber sido tomada como verdad sin verificar el comportamiento real de `.FullName` en .NET/PowerShell."
  - "Los tests nuevos de GAP C (`test_ready_for_pr_pr_body_references_real_latest_attempt` en tests/test_feature_contract_scripts.py:639-673 y `test_milestone_pr_body_references_real_latest_attempt` en tests/test_milestone_ready_for_pr.py:227-265) no detectan este bug porque solo verifican `assert \"audit-2.md\" in body` y `assert \"audit-N.md\" not in body` — ambas aserciones son ciertas tanto si el path es relativo como si es absoluto (la subcadena 'audit-2.md' aparece igual al final de una ruta absoluta). Ninguno de los dos tests verifica el formato de ruta (ausencia de un path absoluto tipo unidad de disco/segmento del directorio temporal del test), pese a que ese formato es un caso borde explícito de spec.md. Esto es exactamente el patrón de 'test que pasa trivialmente sin ejercitar el caso borde real' señalado en la checklist de code review — no es un falso positivo del AC-6/AC-7 en sí (la sustitución del literal SÍ funciona), pero sí deja sin cobertura el requisito de 'sin rutas absolutas' que la propia spec pide explícitamente."
  - "test-report-1.md sección 6 ('Intentos rechazados intermedios / encoding de rutas') repite la misma afirmación incorrecta ('Get-LatestVerdictArtifact ya devuelve rutas relativas runs/<slug>/...') sin haber impreso/inspeccionado el contenido real de `captured-body.md`/`captured_body` en los tests para confirmar el formato de la ruta — la verificación de QA sobre este punto específico fue por lectura de código (mal interpretada), no empírica, a diferencia del resto de su reporte."
  - "Corrección sugerida para builder-agent (no prescriptiva de implementación, solo el defecto a resolver): en el punto donde `ready-for-pr.ps1` arma `$evidenceSection`/el checklist, convertir `$auditArtifact.Path`/`$qaArtifact.Path`/`$codeReviewArtifact.Path` a una ruta relativa consistente con `$info.RunDir` antes de interpolarlos en el body (o ajustar `Get-LatestVerdictArtifact` para que devuelva la ruta relativa que ya recibe en `-Directory`, revisando el resto de sus consumidores en `Assert-LatestVerdictApproved` para no romper sus mensajes de error). Además, extender ambos tests de GAP C (`test_ready_for_pr_pr_body_references_real_latest_attempt` y `test_milestone_pr_body_references_real_latest_attempt`) para que fallen si el body contiene el separador de unidad de disco Windows (`:\\`) o el path absoluto del `tmp_path` del propio test, no solo para chequear la subcadena del nombre del archivo."
  - "Observación no bloqueante (heredada de audit-1.md, sección GAP A / caso borde 'Paginación de reviews' de spec.md): ningún test nuevo construye realmente un escenario de 2+ páginas reales (el fake gh de tests/test_complete_approved_pr_script.py siempre devuelve el array de reviews envuelto en un único elemento exterior, ej. `[[{...}]]` o `[[{...},{...}]]` — dos reviews en UNA página, no dos páginas). La lectura del código (`Get-LatestApprovedReviewCommit` + el doble `ConvertTo-ObjectArray`) sugiere que el aplanado sí generalizaría correctamente a un array de 2+ páginas reales, pero eso no está verificado por ningún test con esa forma estructural distinta, pese a que AC-8 pide explícitamente tests para 'casos borde' y spec.md lista la paginación como caso a contemplar. No bloquea por sí solo esta revisión (ya era una observación no bloqueante de audit-1.md), pero sigue pendiente."
  - "Nota menor, no bloqueante: runs/v1.1.0/02-integridad-post-hitl-y-ready-for-pr/decision.md lista `code-review-1.md` (este mismo archivo) bajo 'Evidencias revisadas', pero ese archivo no existía en el momento en que builder-agent escribió decision.md (commit 1e1501e, anterior a test-report-1.md y a esta revisión). 'Evidencias revisadas' debería limitarse a lo que realmente se leyó al momento de escribir decision.md, o aclarar que son los artefactos esperados del circuito, no una afirmación de revisión retroactiva de un archivo aún inexistente."
```

## Resumen de la auditoría técnica

Revisado el diff final completo entre `45fde47` y HEAD del worktree `D:\proyectos\worktrees\02-integridad-post-hitl-y-ready-for-pr`: `scripts/complete-approved-pr.ps1`, `scripts/ready-for-pr.ps1`, `tests/test_complete_approved_pr_script.py`, `tests/test_feature_contract_scripts.py`, `tests/test_milestone_ready_for_pr.py`, `docs/tecnica/integridad-post-hitl-y-ready-for-pr.md`, `docs/usuario/integridad-post-hitl-y-ready-for-pr.md`, índices, `decision.md`, `spec.md`/`plan.md`/`tasks.md`/`audit-1.md`/`test-report-1.md`.

**GAP A** (`scripts/complete-approved-pr.ps1:200-246, 341-362`): correcto. Se verificó manualmente, línea por línea, el comportamiento de `Get-LatestApprovedReviewCommit` contra los casos borde de cero reviews, una sola review, y múltiples reviews en una sola "página" simulada — el doble `ConvertTo-ObjectArray` (a nivel de "páginas" y a nivel de "reviews dentro de una página") es robusto frente a la conocida peculiaridad de PowerShell de colapsar un array de un solo elemento al capturarlo desde un pipeline, y produce el resultado correcto en todos los casos verificables analíticamente. La inserción de la validación stale/no-stale entre `reviewDecision` y `Wait-PrChecks` es correcta y no deja ningún camino hacia `gh pr merge` sin pasar por ella. Único déficit: no hay test con la forma estructural real de 2+ páginas (no bloqueante, ya señalado por audit-1.md).

**GAP B** (`scripts/ready-for-pr.ps1:128-196`): correcto y simétrico entre Feature y Milestone, incluidas las ramas "ya Ready"/"todos ya Ready". La validación temprana (`Assert-FeatureContract`/`Assert-WorkUnitContract` sin `-RequireReadyRoadmap`) corre antes de cualquier mutación/commit en ambas ramas. Los tests `test_ready_for_pr_blocks_roadmap_mutation_when_contract_fails` y `test_milestone_ready_for_pr_blocks_roadmap_mutation_when_contract_fails` son sólidos (comparan bytes de `ROADMAP.md` y `git log --oneline` antes/después, no solo código de salida).

**GAP C** (`scripts/ready-for-pr.ps1:213-220` y `scripts/feature-contract.ps1:72-146`): **bug real**. `Get-LatestVerdictArtifact` devuelve `.FullName` (ruta absoluta), no una ruta relativa, contradiciendo el caso borde explícito de `spec.md` ("sin rutas absolutas del entorno del agente") y la propia documentación técnica escrita por builder-agent (que afirma, incorrectamente, que la ruta ya es relativa). Los tests nuevos de GAP C no lo detectan porque solo comprueban la presencia/ausencia de la subcadena del nombre de archivo, no el formato de la ruta completa.

No hay scope creep (nada de MILESTONE size-limit, workflows de Actions, ni router de modelos tocado), ningún test preexistente fue debilitado (confirmado por lectura directa de los diffs de test y coincide con lo reportado por QA), y `decision.md` no afirma aprobación de merge (correcto conforme a AGENTS.md), salvo la nota menor sobre la referencia anticipada a `code-review-1.md`.

**Veredicto: rejected**, por el bug concreto de GAP C (rutas absolutas filtradas al cuerpo público de la PR, en violación directa de un caso borde explícito de `spec.md`) y la documentación técnica que describe incorrectamente ese comportamiento. Corresponde volver a `builder-agent` (paso 3 de AGENTS.md); dado que el fix probablemente toque `scripts/ready-for-pr.ps1` y/o los tests de GAP C, el circuito debe volver a pasar por `qa-agent` antes de la próxima pasada de code review.
