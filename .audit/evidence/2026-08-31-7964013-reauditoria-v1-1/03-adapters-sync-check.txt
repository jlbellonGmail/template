Comando oficial (AGENTS.md, ci.yml linea 29): pwsh -NoProfile
-ExecutionPolicy Bypass -File ./scripts/sync-agentic-adapters.ps1
-Check

Nota metodologica -- falso positivo del auditor, corregido: la primera
ejecucion se hizo por error con `powershell` (Windows PowerShell 5.1,
powershell.exe) en vez de `pwsh` (PowerShell 7), y fallo con:
  Adaptador desactualizado o divergente: .mcp.json
  Adaptador desactualizado o divergente: opencode.json
  EXIT CODE: 1

AGENTS.md ("Herramientas locales requeridas"), README.md
("Compatibilidad de plataforma") y ci.yml especifican explicitamente
`pwsh` (PowerShell 7), no `powershell.exe` (Windows PowerShell 5.1
Desktop). Se investigo la causa antes de puntuar (AUDIT_RULES.md #19,
Caso B): ConvertTo-Json produce formato distinto entre las dos
ediciones de PowerShell para objetos vacios/anidados, generando una
divergencia de texto que no existe realmente en el estado del
repositorio.

Repeticion con el interprete correcto (pwsh, PowerShell 7.6.4, igual
que el runner de ci.yml):

Comando: pwsh -NoProfile -ExecutionPolicy Bypass -File
./scripts/sync-agentic-adapters.ps1 -Check
Resultado: Adaptadores agenticos sincronizados.
EXIT CODE: 0

Estado: VERIFICADO -- PASS. Sin drift real entre .agentic/*.json y los
adaptadores generados (.claude/agents/*.md, .codex/*.config.toml,
.codex/config.toml, .mcp.json, opencode.json, CLAUDE.md, mirrors de
skills). El falso positivo inicial fue un error de tooling del propio
auditor (interprete incorrecto), no un defecto del repositorio.
